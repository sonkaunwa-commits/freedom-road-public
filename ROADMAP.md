# Freedom Road Roadmap

> 本文件记录可公开的工程化 Roadmap 与发布质量要求。涉及私人账户、密钥、服务器敏感信息、Private Ledger 等内容不写入本公开仓库。

## 2026-08-22：用户页面发布质量门禁（Release Quality Gate）

### 背景
2026-08-22 基金小帮手迭代过程中出现了“仓库代码已更新，但 GitHub Pages 线上仍为旧版 / 404 / 缓存未刷新”的问题。由此将“发布前测试与线上验收”升级为 Freedom Road 所有用户可见网页和工具的强制工程规则。

### 强制规则
以后任何用户可见页面（基金助手、考试页面、投资页面、Freedom Road 公共入口及后续新工具）都必须满足以下条件后，才允许对用户表述为“已发布 / 已上线 / 已更新完成”：

1. **代码检查**
   - HTML / CSS / JavaScript 基础语法检查通过。
   - 关键 DOM、按钮、入口、资源路径无明显错误。
   - 新增文件必须确认已提交到正确仓库、正确分支、正确目录。

2. **构建检查**
   - 页面依赖的构建脚本、数据生成脚本、静态资源生成流程必须成功。
   - 不允许无关模块失败阻断当前页面发布；发布流程应尽量按模块解耦。

3. **部署检查**
   - GitHub Pages / 服务器部署任务必须明确成功。
   - 不以“Git commit 成功”代替“线上部署成功”。
   - 连续提交时不得因并发取消、旧构建覆盖新构建而造成版本倒退。

4. **线上真实 URL 验收**
   - 必须访问用户实际打开的正式 URL，而不是只检查仓库文件。
   - 正式 URL 必须返回 200，不能出现 404、空白页、循环跳转或资源 404。
   - 必须确认线上页面包含本次发布的版本标记 / release marker。
   - 页面依赖的关键 JS / CSS / JSON 也必须逐项确认可访问。

5. **缓存与版本验收**
   - 重要前端资源使用明确版本号或不可变文件名。
   - 对高风险缓存场景（Safari / GitHub Pages CDN / Service Worker）必须主动做 cache-busting。
   - 页面应显示可读的“界面版本 / 数据更新时间”，便于判断用户实际看到的是哪一版。

6. **移动端 / 真实使用检查**
   - 至少检查常用手机宽度下的排版、按钮可点击性、文字可读性、输入框和滚动体验。
   - 面向老年用户的页面必须额外检查：字号、按钮辨识度、点击区域、术语是否易懂。

7. **业务逻辑检查**
   - 涉及交易日、节假日、时间窗口、行情日期、净值日期的页面必须核对真实日期语义。
   - 非交易日不得显示成“今天可以买 / 今天下午操作”之类交易日指令。
   - 金融数据缺失或过期时必须显式降级，不得伪装成最新数据。

8. **发布完成定义（Definition of Done）**
   - 只有“代码检查通过 + 构建通过 + 部署成功 + 正式 URL 线上验收通过 + 关键交互检查通过”后，任务才算完成。
   - 任一项未通过，只能表述为“代码已提交 / 正在部署 / 尚未完成线上验收”，不得提前告诉用户“已经更新好了”。

### 后续工程化
- 为每个公开页面增加统一的 release marker 与版本显示。
- 建立可复用的 smoke test：HTTP 200、关键文本、关键资源、主要按钮、数据 JSON。
- 将 Pages / 服务器部署结果纳入统一健康检查。
- 对基金助手、考试页面等高频页面逐步增加自动化移动端截图或浏览器验收。
- 将“发布后线上验收失败”纳入告警，不允许静默失败。

### 优先级
**P0 / 长期强制规则**。从 2026-08-22 起适用于 Freedom Road 之后所有用户可见页面发布。
