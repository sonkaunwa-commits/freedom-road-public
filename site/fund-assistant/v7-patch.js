(()=>{
const d=document,$=s=>d.querySelector(s),$$=s=>[...d.querySelectorAll(s)],HK='mom_funds_v6',WK='mom_fund_watch_v1';
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const N=v=>{if(v===null||v===undefined||String(v).trim()==='')return null;const x=Number(v);return Number.isFinite(x)?x:null};
const P=v=>v===null||!Number.isFinite(v)?'—':`${v>0?'+':''}${v.toFixed(1)}%`,V=v=>v===null||!Number.isFinite(v)?'—':Number(v).toFixed(4);
const css=`.learn{margin-top:10px}.learn details{border-top:1px solid #edf0ec;padding:11px 0}.learn details:first-child{border-top:0}.learn summary{font-size:18px;font-weight:900;cursor:pointer;line-height:1.5}.learn p{font-size:16px;line-height:1.8;color:#4f5851;margin:7px 0 0}.track-note{font-size:15px;line-height:1.75;color:#5f6861;background:#f5f7f4;border-radius:12px;padding:10px 11px;margin-top:9px}.watchlist{display:grid;gap:10px;margin-top:12px}.watchitem{border:1px solid var(--line);border-radius:16px;padding:13px;background:#fff}.budget{background:#f5f7f4;border-radius:14px;padding:12px;margin-top:10px}.budget-row{display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:8px}.budget-result{font-size:16px;line-height:1.8;margin-top:9px}.protect{background:#eef6f1;border-radius:13px;padding:12px;margin-top:9px;font-size:16px;line-height:1.8}.eta{background:#eef4fb;border-radius:13px;padding:12px;margin-top:10px;font-size:16px;line-height:1.75}.minor{font-size:14px;color:var(--muted);line-height:1.65}.buttons .add{max-width:100%;white-space:normal}.source{font-size:13px!important}.line{font-size:17px!important}.plan h3{font-size:19px!important}`;
const st=d.createElement('style');st.textContent=css;d.head.appendChild(st);
const sub=$('.sub');if(sub)sub.textContent='重点告诉你：选什么、今天买不买、买了以后怎么把利润尽量保护住。';
const pick=$('#pick'),firstCard=pick?.querySelector('.card');
if(firstCard){const desc=firstCard.querySelector('.desc'),notice=firstCard.querySelector('.notice');if(desc)desc.textContent='不是今天推荐、明天就换。先连续跟踪，只有基金本身、趋势或同类竞争力明显变差才换掉。';if(notice)notice.innerHTML='<b>系统怎么分析：</b>先看过去半年和一年有没有持续竞争力；再看最近1–3个月是不是涨得太快、跌得太狠；还会看近20、60、120个交易日走势、近期最大回撤和波动风险，最后才给“今天买一点、继续等、还是不要碰”的结论。<br><b>最重要：</b>过去涨得多只代表值得研究，不代表今天就该买。';}
if(pick&&!$('#watchlist')){const watch=d.createElement('div');watch.className='card';watch.innerHTML='<div class="title">我的重点跟踪</div><div class="desc"><b>还没买也可以加。</b>这里只代表“我想继续观察这只”，系统以后帮你反复看今天是不是更适合买。<br><b>真正买了以后</b>，请到“我的基金”填成本和金额，系统才知道什么时候该保护利润、什么时候该考虑卖。</div><div id="watchlist" class="watchlist"></div>';pick.appendChild(watch);const learn=d.createElement('div');learn.className='card';learn.innerHTML='<div class="title">新手先懂这4件事</div><div class="desc">不用学很多理论，先把这四件事弄懂，买基金就不容易乱。</div><div class="learn"><details open><summary>1. 净值是什么？</summary><p>可以把净值理解成这只基金“每一份值多少钱”。开放式基金不是像股票那样随时能锁定一个实时成交价，正式净值一般要等当天收市后计算出来，所以白天看到的估值只能作参考。</p></details><details><summary>2. 为什么经常让你下午2:20再看？</summary><p>很多场外开放式基金在交易日15:00前提交申购或赎回，通常按当天净值确认；15:00以后通常顺延到下一个交易日。2:20左右再看一次，是为了更接近当天收盘情况，同时给自己留一点操作时间。特殊基金以自己的合同和平台提示为准。</p></details><details><summary>3. 为什么不建议一次把钱全买进去？</summary><p>因为再好的基金也可能继续跌。比如准备投2万元，先买5000元左右，后面真的回调到更舒服的位置再买5000元，至少留一半资金做后手，比一次全部买完更容易控制风险。</p></details><details><summary>4. 赚了以后为什么还要设“利润保护线”？</summary><p>赚钱以后最怕从盈利又一路跌回亏损。系统会根据你的成本、已经赚了多少和基金趋势给一个利润保护参考线。它不能保证一定不亏，但会提醒你什么时候先卖一部分，把已经赚到的钱尽量留下来。</p></details></div>';pick.appendChild(learn);}
const mineDesc=$('#mine .desc');if(mineDesc)mineDesc.textContent='最重要的是三项：基金代码、买入成本、大概投入金额。这样系统才能更具体地告诉你“现在赚亏多少、卖多少比较合适”。买入日期是可选项，用来提醒短期赎回费；总资产不用填。';
const labels=$$('#mine .label');labels.forEach(x=>{if(x.textContent.startsWith('2. 大概投入多少钱'))x.textContent='2. 大概投入多少钱（建议填）';if(x.textContent.startsWith('3. 买入成本净值'))x.textContent='3. 买入成本净值（很建议填）';});
const footer=$('.footer');if(footer)footer.textContent='研究辅助，不保证收益，也不能保证本金一定不亏。目标是尽量避免追高、控制补仓，并在盈利后通过分批止盈和保护线尽量把利润留下来。最终以基金公司/销售平台确认规则为准。';
function stat7(f){const a=f.a,n=a.length,last=a[n-1].v,at=k=>a[Math.max(0,n-1-k)].v,ma=k=>a.slice(Math.max(0,n-k)).reduce((s,x)=>s+x.v,0)/Math.min(k,n),hi=k=>Math.max(...a.slice(Math.max(0,n-k)).map(x=>x.v));const rr=a.slice(1).map((x,i)=>(x.v/a[i].v-1)*100),r20s=rr.slice(-20),avgAbs=r20s.length?r20s.reduce((s,x)=>s+Math.abs(x),0)/r20s.length:.6,mean=r20s.length?r20s.reduce((s,x)=>s+x,0)/r20s.length:0,vr=r20s.length>1?r20s.reduce((s,x)=>s+(x-mean)**2,0)/(r20s.length-1):0,vol=Math.sqrt(vr)*Math.sqrt(250);const m20=ma(20),m60=ma(60),m120=ma(120),r20=(last/at(20)-1)*100,r60=(last/at(60)-1)*100,r120=(last/at(120)-1)*100,r250=n>251?(last/at(250)-1)*100:null,dd=(last/hi(60)-1)*100,ref=N(f.est?.gsz)||last,dev=(ref/m60-1)*100;let peak=-Infinity,maxdd=0;for(const x of a.slice(-Math.min(250,n))){peak=Math.max(peak,x.v);maxdd=Math.min(maxdd,(x.v/peak-1)*100)}const trend=last>m20&&m20>m60&&m60>m120?'明显向上':last>m60&&r60>0?'总体向上':last>m120?'来回震荡':'偏弱',near=[m20*.985,m20*1.01],deep=[m60*.975,m60*1.008],stop=m120*.98,risk=(vol>35||maxdd<-35)?'波动很大':(vol>22||maxdd<-22)?'波动较大':'波动中等';let action='今天先不买',tone='wait',why='现在没有到特别舒服的位置。继续等，比为了怕错过而追进去更好。',how='今天不操作，下午2:20左右再看一次。';if(ref<stop||trend==='偏弱'){action='今天不要买，也不要补';tone='stop';why='走势已经偏弱。跌得多不代表一定便宜，先等它重新稳定。';how='今天不加钱。已经持有的先看是否跌破中期保护位置。'}else if((r20>10&&dev>8)||ref>near[1]*1.055){action='今天不要追';tone='wait';why='最近涨得比较快，现在买进去容易买在高处。';how='继续等回落。如果下午仍然很强也不追，宁可错过一小段。'}else if(ref>=deep[0]&&ref<=deep[1]&&trend.includes('向上')){action='今天可以少买一点';tone='buy';why='已经回到比较舒服的位置，而且中期走势还没有坏。';how='下午2:20左右如果还在这个位置，可以先买准备金额的约20%–25%。'}else if(ref>=near[0]&&ref<=near[1]&&trend.includes('向上')){action='今天可以先买一点';tone='buy';why='趋势还在，位置也比较合理。';how='下午2:20左右如果还在这个位置，可以先买准备金额的约20%–25%，不要一次买满。'}let eta='';if(tone==='buy')eta='今天已经到可以考虑小额买入的位置，下午2:20左右再确认一次即可。';else if(trend==='偏弱'||ref<stop)eta='现在不是“再等几天就买”的问题。先观察至少3–5个交易日，看走势能不能重新稳定，再重新判断。';else if(ref>near[1]){const gap=(ref/near[1]-1)*100,days=gap/Math.max(.25,avgAbs),lo=Math.max(1,Math.floor(days*.7)),hh=Math.max(lo+1,Math.ceil(days*1.8));eta=`按最近20个交易日的平均波动粗略估算，如果出现正常回调，大约${lo}–${hh}个交易日可能靠近更舒服的位置。只是估算，不是预测日期。`;}else eta='已经回调到较低位置附近，但先不要急着抄底；再观察1–3个交易日能否止跌、走势能否稳定。';return{last,ref,m20,m60,m120,r20,r60,r120,r250,dd,dev60:dev,trend,near,deep,stop,action,tone,why,how,maxdd250:maxdd,riskLevel:risk,eta};}
function daysHeld(x){if(!x)return null;const t=new Date(x+'T00:00:00').getTime();return Number.isFinite(t)?Math.max(0,Math.floor((Date.now()-t)/86400000)):null}
function personal7(s,cost,pnl,date){const p=cost?((s.ref/cost)-1)*100:pnl;let action=s.action,tone=s.tone,why=s.why,how=s.how,days=daysHeld(date);if(p!==null){if(p<=-18&&(s.trend==='偏弱'||s.ref<s.stop)){action='先别再补，开始准备退出';tone='stop';why='已经亏得比较多，而且走势也弱。继续补很容易越套越深。';how='今天不补。后面如果反弹但趋势仍弱，可考虑先减一部分；如果继续跌破保护位置，更要重新评估是否退出。'}else if(p<=-8&&s.tone==='buy'){action='可以小补，但别重仓';tone='buy';why='这次回调还没有破坏中期走势，而且到了相对舒服的位置。';how='只补原计划金额的约10%–20%，其余资金继续留着。'}else if(p>=50){action='赚得很多了，开始保护利润';tone='wait';why='已经有较大盈利，重点从“还能涨多少”转为“不要把利润全部跌回去”。';how='如果近期又快速上涨，可先卖约25%–30%；剩余继续持有，趋势明显转弱时再继续减。'}else if(p>=25&&s.r20>10){action='可以先卖一点';tone='wait';why='已经赚了不少，而且最近涨得偏快。';how='可以先卖约20%–25%锁定一部分利润，剩下继续跟踪。'}else if(p>0&&s.trend==='偏弱'){action='先卖一点保护利润';tone='wait';why='已经赚钱，但走势开始变弱。';how='可以先减约20%–25%，避免已经到手的利润又全部跌回去。'}}if(days!==null&&days<7)how+=' 另外，你买入还不到7天，卖出前先在销售平台查看短期赎回费。';return{p,action,tone,why,how,days}}
function protect7(s,cost,p){if(!cost||p===null)return null;let line,label;if(p>=50){line=Math.max(cost*1.25,s.m60*.98);label='高盈利保护线'}else if(p>=30){line=Math.max(cost*1.15,s.m60*.98);label='利润保护线'}else if(p>=15){line=Math.max(cost*1.08,s.m60*.98);label='利润保护线'}else if(p>=8){line=Math.max(cost*1.02,s.m60*.98);label='尽量保本线'}else if(p>0){line=cost*1.005;label='接近成本保护线'}return line?{line,label}:null}
function budgetText(a,s){a=N(a);if(!a||a<=0)return'请输入你准备投这只基金的金额，例如20000。';if(s.tone==='stop')return`你准备了 ${Math.round(a).toLocaleString()} 元，但今天建议买 0 元。先把钱留着，等走势重新稳定。`;if(s.tone==='buy')return`如果总预算是 ${Math.round(a).toLocaleString()} 元：今天先买约 ${Math.round(a*.25).toLocaleString()} 元；以后再回调且走势没坏，再买约 ${Math.round(a*.25).toLocaleString()} 元；至少留 ${Math.round(a*.5).toLocaleString()} 元备用。`;return`如果总预算是 ${Math.round(a).toLocaleString()} 元：今天先不买。以后出现合适位置，第一笔约 ${Math.round(a*.25).toLocaleString()} 元，第二笔约 ${Math.round(a*.25).toLocaleString()} 元，至少留 ${Math.round(a*.5).toLocaleString()} 元备用。`}
function chart7(a){const z=a.slice(-120),vs=z.map(x=>x.v),mn=Math.min(...vs),mx=Math.max(...vs),w=320,h=112,p=7,pts=z.map((x,i)=>`${p+i*(w-2*p)/Math.max(1,z.length-1)},${h-p-(x.v-mn)/(mx-mn||1)*(h-2*p)}`).join(' ');return`<div class="chart"><div class="charthead"><span>近120个交易日净值走势</span><span>${mn.toFixed(3)}–${mx.toFixed(3)}</span></div><svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><polyline fill="none" stroke="#4b806a" stroke-width="2.2" points="${pts}"/></svg></div>`}
function decision7(f,s,o={}){const p=personal7(s,o.cost??null,o.pnl??null,o.date??null),p1=`${V(s.near[0])}～${V(s.near[1])}`,p2=`${V(s.deep[0])}～${V(s.deep[1])}`,pr=protect7(s,o.cost??null,p.p);return`<div class="fund"><div class="top"><div><div class="name">${esc(f.name)}</div><div class="code">${f.code}${f.mgr?' · 基金经理 '+esc(f.mgr):''}</div></div><span class="tag ${p.tone}">${esc(p.action)}</span></div><div class="answer ${p.tone}"><div class="a1">今天最简单的结论</div><div class="a2">${esc(p.action)}</div><div class="a3">${esc(p.why)}</div><div class="a3"><b>今天怎么做：</b>${esc(p.how)}</div></div><div class="eta"><b>大概什么时候可能更适合买？</b><br>${esc(s.eta)}</div><div class="metrics"><div class="m"><div class="k">最新正式净值</div><div class="v">${V(s.last)}</div></div><div class="m"><div class="k">近60日涨跌</div><div class="v">${P(s.r60)}</div></div><div class="m"><div class="k">离近期高点</div><div class="v">${P(s.dd)}</div></div><div class="m"><div class="k">风险感觉</div><div class="v">${s.riskLevel}</div></div></div><div class="plan"><h3>这只基金现在是什么状态？</h3><div class="line"><b>中期走势：</b>${s.trend}。</div><div class="line"><b>近20日：</b>${P(s.r20)}；<b>近120日：</b>${P(s.r120)}${s.r250!==null?`；<b>近约1年：</b>${P(s.r250)}`:''}。</div><div class="line"><b>近一年最大回撤参考：</b>${P(s.maxdd250)}。波动越大，越不能一次买满。</div></div><div class="plan"><h3>如果还没买：怎么等、怎么买？</h3><div class="line"><b>先等到大约 ${p1}</b>，下午2:20再看走势是否还稳。</div><div class="line">如果之后再回到 <b>${p2}</b> 左右，但趋势还没有坏，可以考虑第二次小额买。</div><div class="line">如果跌到 <b>${V(s.stop)}</b> 以下：<b>先停手，不继续补。</b></div><div class="budget"><b>按你的预算，具体买多少？</b><div class="minor">只填你准备投这一只基金的金额，不是家庭总资产。</div><div class="budget-row"><input class="input budget-input" inputmode="decimal" placeholder="例如 20000"><button class="btn budget-btn">帮我算</button></div><div class="budget-result"></div></div></div><div class="plan"><h3>如果已经买了：什么时候卖？</h3>${o.cost?`<div class="line">你的成本约 <b>${V(o.cost)}</b>${p.p!==null?`，当前大约 ${P(p.p)}`:''}。</div>`:'<div class="line">想得到更准确的卖出建议，最好在“我的基金”里填买入成本。</div>'}<div class="line">赚钱以后如果短期突然快速上涨：先卖约20%–30%，把一部分利润真正落袋。</div><div class="line">盈利达到25%–50%以上：重点改成保护利润，不再无计划追高加仓。</div><div class="line">趋势明显转弱：即使还没亏，也要考虑先减一部分。</div>${pr?`<div class="protect"><b>${pr.label}参考：${V(pr.line)}</b><br>如果净值回落到这附近，而且中期走势也在变弱，可以考虑先卖一部分。目标是尽量不让已经赚到的利润又全部跌回去，但<b>不能保证一定不亏</b>。</div>`:''}</div><details class="why" open><summary>我是怎么做这个判断的？</summary><ul><li>看20、60、120个交易日趋势，不只看今天涨跌。</li><li>看离近期高点有多远，尽量避免大涨以后才追进去。</li><li>看近一年最大回撤和最近波动，判断这只基金到底有多“折腾”。</li><li>结合基金经理；推荐池还会结合半年、一年表现和同类竞争力持续筛选。</li><li>填了成本、金额和买入日期后，会把你的真实盈利、持有时间和预算一起算进去。</li></ul></details>${chart7(f.a)}<div class="source">净值是基金每份资产的价值。盘中估值不等于最终净值；确认时间和费用以基金合同及销售平台为准。</div></div>`}
function bind(root,s,o={}){const i=root.querySelector('.budget-input'),b=root.querySelector('.budget-btn'),out=root.querySelector('.budget-result');if(!i||!b||!out)return;if(o.amount)i.value=o.amount;const go=()=>out.textContent=budgetText(i.value,s);b.onclick=go;if(o.amount)go()}
window.stat=stat7;window.decisionHtml=decision7;
let pnlType='unknown';const pt=$('#pnlType');pt?.querySelectorAll('button').forEach(b=>b.onclick=()=>{pnlType=b.dataset.v;pt.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#pnlField')?.classList.toggle('hidden',pnlType==='unknown')});
function getH(){try{return JSON.parse(localStorage.getItem(HK)||'[]')}catch{return[]}}function setH(a){localStorage.setItem(HK,JSON.stringify(a));renderH()}
function renderH(){const box=$('#holdings'),a=getH();if(!box)return;if(!a.length){box.innerHTML='<div class="empty">还没有保存基金。最少填一个6位基金代码就可以开始。</div>';return}box.innerHTML='';a.forEach(x=>{const el=d.createElement('div');el.className='fund';el.innerHTML=`<div class="top"><div><div class="name">基金 ${x.code}</div><div class="code">${x.amount?'投入约 '+Number(x.amount).toLocaleString()+'元':'未填投入金额'}${x.cost?' · 成本 '+V(x.cost):''}${x.date?' · 买入 '+esc(x.date):''}</div></div><button class="btn secondary del">删除</button></div><div class="buttons"><button class="btn check">更新今天情况</button></div><div class="live"></div>`;box.appendChild(el);el.querySelector('.del').onclick=()=>setH(getH().filter(y=>y.code!==x.code));el.querySelector('.check').onclick=async()=>{const live=el.querySelector('.live');live.innerHTML='<div class="loader">正在查最新数据并分析…</div>';try{const f=await window.fund(x.code),s=stat7(f);live.innerHTML=decision7(f,s,x);bind(live,s,x)}catch(e){live.innerHTML=`<div class="risk">${esc(e.message)}</div>`}}})}
const save=$('#save');if(save)save.onclick=()=>{const code=$('#hCode').value.trim(),amount=N($('#hAmount').value),cost=N($('#hCost').value),date=$('#hDate').value||null,abs=N($('#hPnlAbs').value);if(!/^\d{6}$/.test(code))return alert('请先填6位基金代码');let pnl=null;if(pnlType==='gain'&&abs!==null)pnl=Math.abs(abs);if(pnlType==='loss'&&abs!==null)pnl=-Math.abs(abs);const a=getH().filter(x=>x.code!==code);a.unshift({code,amount,cost,date,pnl});setH(a);$('#hCode').value=$('#hAmount').value=$('#hCost').value=$('#hPnlAbs').value='';$('#hDate').value='';pnlType='unknown';pt.querySelectorAll('button').forEach(x=>x.classList.toggle('active',x.dataset.v==='unknown'));$('#pnlField').classList.add('hidden')};
function getW(){try{return JSON.parse(localStorage.getItem(WK)||'[]')}catch{return[]}}function setW(a){localStorage.setItem(WK,JSON.stringify(a));renderW()}function addW(x){const a=getW().filter(y=>y.code!==x.code);a.unshift({code:x.code,name:x.name||('基金 '+x.code)});setW(a)}
function renderW(){const box=$('#watchlist'),a=getW();if(!box)return;if(!a.length){box.innerHTML='<div class="empty">还没有重点跟踪。看到感兴趣的推荐基金，点“加入重点跟踪（没买也可以）”就会放到这里。</div>';return}box.innerHTML='';a.forEach(x=>{const el=d.createElement('div');el.className='watchitem';el.innerHTML=`<div class="top"><div><div class="name">${esc(x.name)}</div><div class="code">${x.code} · 还没买也可以继续观察</div></div><button class="btn secondary remove">移除</button></div><div class="track-note">如果只是想等更好的价格，就留在这里；已经买了，请点“我已经买了”，到“我的基金”填成本和金额。</div><div class="buttons"><button class="btn update">更新今天情况</button><button class="btn secondary bought">我已经买了</button></div><div class="live"></div>`;box.appendChild(el);el.querySelector('.remove').onclick=()=>setW(getW().filter(y=>y.code!==x.code));el.querySelector('.bought').onclick=()=>{$('#hCode').value=x.code;$$('.tab')[1].click()};el.querySelector('.update').onclick=async()=>{const live=el.querySelector('.live');live.innerHTML='<div class="loader">正在更新今天情况…</div>';try{const f=await window.fund(x.code),s=stat7(f);live.innerHTML=decision7(f,s);bind(live,s)}catch(e){live.innerHTML=`<div class="risk">${esc(e.message)}</div>`}}})}
async function loadPool7(){const box=$('#pool');try{const r=await fetch(`data/opportunity.json?v=${Date.now()}`);if(!r.ok)throw Error('推荐池数据未加载');const data=await r.json();$('#state').textContent=`推荐池更新：${(data.generated_at||'').replace('T',' ').slice(0,16)} · ${data.funds?.length||0}只重点跟踪`;box.innerHTML='';for(const x of data.funds||[]){const rs=x.returns||{},el=d.createElement('div');el.className='fund';el.innerHTML=`<div class="top"><div><div class="name">${esc(x.name)}</div><div class="code">${x.code} · ${esc(x.category||'')} · 已跟踪${x.tracking?.days_tracked||1}天</div></div><span class="tag wait">${esc(x.simple_status||'持续观察')}</span></div><div class="metrics"><div class="m"><div class="k">近1月</div><div class="v">${P(N(rs['1m']))}</div></div><div class="m"><div class="k">近3月</div><div class="v">${P(N(rs['3m']))}</div></div><div class="m"><div class="k">近6月</div><div class="v">${P(N(rs['6m']))}</div></div><div class="m"><div class="k">近1年</div><div class="v">${P(N(rs['1y']))}</div></div></div><details class="why" open><summary>为什么把它选进来？</summary><ul>${(x.why||[]).map(y=>`<li>${esc(y)}</li>`).join('')}</ul></details><div class="risk"><b>主要风险：</b>${esc(x.risk||'高弹性基金波动较大。')}</div><div class="buttons"><button class="btn look">更新今天情况</button><button class="btn secondary add">加入重点跟踪（没买也可以）</button></div><div class="source">点“更新今天情况”会重新读取最新公开走势。加入重点跟踪不代表已经买入；真正买了以后再到“我的基金”填成本和金额。</div><div class="live"></div>`;box.appendChild(el);el.querySelector('.look').onclick=async()=>{const live=el.querySelector('.live');live.innerHTML='<div class="loader">正在更新今天情况…</div>';try{const f=await window.fund(x.code),s=stat7(f);live.innerHTML=decision7(f,s);bind(live,s)}catch(e){live.innerHTML=`<div class="risk">${esc(e.message)}</div>`}};el.querySelector('.add').onclick=()=>{addW(x);el.querySelector('.add').textContent='已加入重点跟踪'}}}catch(e){box.innerHTML='<div class="empty">推荐基金暂时没有加载出来，可以先到“查一只基金”输入代码查询。</div>'}}
const go=$('#go');if(go)go.onclick=async()=>{const code=$('#dCode').value.trim(),box=$('#result');if(!/^\d{6}$/.test(code)){box.innerHTML='<div class="risk">请先输入6位基金代码。</div>';return}box.innerHTML='<div class="loader">正在查最新走势并给今天的建议…</div>';const q=$('#q').value,m=q.match(/亏(?:了)?(\d+(?:\.\d+)?)%?/),g=q.match(/(?:赚|盈利)(?:了)?(\d+(?:\.\d+)?)%?/),c=q.match(/成本(?:是|约|大概)?\s*(\d+(?:\.\d+)?)/),pnl=m?-Number(m[1]):g?Number(g[1]):null,cost=c?Number(c[1]):null;try{const f=await window.fund(code),s=stat7(f);box.innerHTML=decision7(f,s,{cost,pnl});bind(box,s,{cost,pnl})}catch(e){box.innerHTML=`<div class="risk">${esc(e.message)}</div>`}};
renderH();renderW();loadPool7();
})();